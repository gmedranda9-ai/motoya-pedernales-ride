import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.poseidon.motoya',
  appName: 'MotoYa',
  webDir: 'dist',
  appUrlScheme: 'com.poseidon.motoya',
};

export default config;
